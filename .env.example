# R2 public URL for themes.json (used at build time)
# In development, leave unset to fall back to local public/themes.json
VITE_THEMES_URL=https://pub-xxxx.r2.dev/themes.json

# GitLab personal access token (optional, increases rate limit for GitLab themes)
# GITLAB_TOKEN=glpat-xxxx
